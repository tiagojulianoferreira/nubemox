# ==============================================================================
# VARIÁVEIS DE AMBIENTE PARA NUBEMOX
# ==============================================================================

# --- CONFIGURAÇÃO DA APLICAÇÃO (Flask) ----------------------------------------
SECRET_KEY="SUA_CHAVE_SECRETA_FORTE_AQUI"
CORS_ORIGINS="http://localhost:3000,http://localhost:5173"

# --- CONFIGURAÇÃO DO BANCO DE DADOS (PostgreSQL) ------------------------------
# Formato: postgresql://<user>:<password>@<host>:<port>/<dbname>
# EX: postgresql://nubemox:password@localhost/nubemox
DATABASE_URL="postgresql://nubemox:password@localhost/nubemox"

# --- CONFIGURAÇÃO DO PROXMOX VE (pve.local) ----------------------------------
PROXMOX_HOST="192.168.0.10"

# 1. AUTENTICAÇÃO PREFERENCIAL: API Token
# Preencha as três variáveis abaixo (USER, TOKEN_NAME, TOKEN_VALUE) para usar o API Token.
# O PROXMOX_USER deve ser o ID completo (ex: meu_usuario@pam, meu_usuario@pve, root@pam).
# O Token Name e Token Value são gerados no Proxmox.
PROXMOX_USER="nubemox@pve"
PROXMOX_API_TOKEN_NAME="nubemox"
PROXMOX_API_TOKEN_VALUE="44283e4f-ec2f-424c-bef6-5b7e84d87ff3"

# 2. AUTENTICAÇÃO DE FALLBACK/TICKET VNC
# Se você usar o API Token acima, a senha só é necessária para gerar tickets VNC.
# Se você NÃO usar o API Token, a conexão principal usará PROXMOX_USER/PROXMOX_PASSWORD.
PROXMOX_PASSWORD="nubemox123"

# 3. CONFIGURAÇÕES GERAIS
# Node Padrão (onde a maioria das ações de criação/listagem ocorrerá)
PROXMOX_DEFAULT_NODE="pve-lab"
# Verificar Certificado SSL. Use 'true' ou 'false'. 'false' é comum em ambientes de teste.
PROXMOX_VERIFY_SSL="false"